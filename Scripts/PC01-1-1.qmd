```{r}
nombre_del_dataset = import(here("nombrecarpeta", "nombre_archivo.extensión"))
 
```

```{r}
install.packages("here")
install.packages("tidyverse")
install.packages("survival")
install.packages("rio")

library(tidyverse)
library(survival)
library(here)
library(rio)
```

---
title: "Sábado"
format: html
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
set.seed(100)
rnorm (16, 46, 21)
```

```{r}
glucosa = c(200, 170, 130, 141)
sd(glucosa) -> Desviación Standard 
median(glucosa) -> mediana

rnorm (n = 16, mean = 46, sd = 21)
rnorm (16, 46, 21)

```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2

data_artritis = import(here("data", "s02_treat_artritis.csv"))
```

The `echo: false` option disables the printing of code (only output is displayed).
